pyspark==3.4.0
delta-spark==2.4.0
kafka-python==2.0.2
ultralytics==8.0.200
# Streamlit >= 1.33 is required for @st.fragment (Phase 4 dashboard).
# Bumped from the original Phase 0 pin of 1.29.
streamlit>=1.33.0,<2.0
pandas==2.1.3
# pyarrow >= 16 is required by deltalake >= 0.20 (older deltalake calls
# pyarrow.field() with the pre-7.x signature, which 16+ rejects).
# Bumped from the original Phase 0 pin of 14.0.1.
pyarrow>=16.0,<20.0
numpy==1.26.2
h5py==3.10.0
pillow==10.1.0
pyyaml==6.0.1
python-dotenv==1.0.0
requests==2.31.0
networkx==3.3
confluent-kafka>=2.4.0
boto3>=1.34.0
jsonschema>=4.21.0
prometheus-client>=0.20.0
# deltalake >= 0.20 fixes the pyarrow.field() compatibility issue that
# silently made the Phase 4 dashboard show empty tables.
deltalake>=0.20,<1.0
streamlit-autorefresh>=1.0.0
pydeck>=0.8.0
lightgbm>=4.3.0,<5.0
onnx>=1.16.0
onnxruntime>=1.17.0
onnxmltools>=1.12.0
onnxconverter-common>=1.14.0
skl2onnx>=1.16.0
mlflow>=2.13.0,<3.0
