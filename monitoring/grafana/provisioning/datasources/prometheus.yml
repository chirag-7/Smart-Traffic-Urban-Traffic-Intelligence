apiVersion: 1

# Auto-provision the Prometheus datasource on first boot so Grafana shows
# data immediately without manual setup.

datasources:
  - name: Prometheus
    uid: prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: true
    jsonData:
      timeInterval: 15s
